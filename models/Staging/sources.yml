version: 2

sources:
  - name: Netflix
    database: movielens
    schema: raw
    tables:
      - name: raw_movies
      - name : raw_tags
      - name : raw_ratings
      - name : raw_genome_tags
      - name : raw_links
      - name : raw_genome_scores